name: ${COMPOSE_PROJECT_NAME:-backend_daily_production}

x-app-common: &app-common
  image: ${APP_IMAGE:-backend-daily-production:local}
  build:
    context: .
  restart: unless-stopped
  env_file:
    - .env
  depends_on:
    mongodb:
      condition: service_healthy

services:
  app:
    <<: *app-common
    ports:
      - "127.0.0.1:${APP_HOST_PORT:-8011}:8000"

  app_blue:
    <<: *app-common
    profiles: ["bluegreen"]
    ports:
      - "127.0.0.1:${APP_BLUE_HOST_PORT:-8011}:8000"

  app_green:
    <<: *app-common
    profiles: ["bluegreen"]
    ports:
      - "127.0.0.1:${APP_GREEN_HOST_PORT:-8013}:8000"

  mongodb:
    image: mongo:7
    init: true
    restart: unless-stopped
    ports:
      - "127.0.0.1:${MONGO_HOST_PORT:-27030}:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASS}
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "bash -ec '</dev/tcp/127.0.0.1/27017'",
        ]
      interval: 30s
      timeout: 3s
      retries: 10
      start_period: 20s
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
